package com.Hilos2;
/*
* Puende haber objetos o métodos sincronizados, un objeto sincronizado solo puede estar siendo utilizado por un hilo a la vez, y lo mismo para los métodos.
* Todos los objetos tienen los métodos wait(), notify() y notifyAll().
*
* */
public class Java_Synchronized {
}
